A heurística de Johnson E1 para o clique máximo pode ser encontrada no arquivo mcp_greedy.cpp. A fim de checar se o clique resultado do algoritmo é realmente um subgrafo completo, foi implementado o código mcp_greedy_checker.cpp


Para a execução, considere < e > como os operadores, respectivamente, de recebimento e envio de fluxo para um arquivo.

Compilação
gpp mcp_greedy.cpp -o mcp_greedy

Execução
./mcp_greedy < instances/instance > result.txt , onde instance é o nome de um dos arquivos das instâncias de teste e result.txt, o nome do arquivo em que ficarão armazenados os resultados do teste
